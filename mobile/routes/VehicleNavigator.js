{/*import React from 'react';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import BicyclePage from '../screens/BicyclePage.js';
import ScooterPage from '../screens/ScooterPage.js';
import CarPage from '../screens/CarPage.js';

const Tab = createBottomTabNavigator();

export default function VehicleNavigator() {
  return (
    <Tab.Navigator initialRouteName="BicyclePage">
      <Tab.Screen name="BicyclePage" component={BicyclePage} />
      <Tab.Screen name="ScooterPage" component={ScooterPage} />
      <Tab.Screen name="CarPage" component={CarPage} />
    </Tab.Navigator>
  );
}*/}